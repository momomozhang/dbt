snapshot-paths:
  - name: scd_raw_hosts
    relation: source('airbnb', 'hosts')
    config:
      unique_key: host_id
      updated_at: updated_at
      strategy: timestamp
      hard_deletes: invalidate
